# --- Seguridad ---
.env
# Backups del .env de producción (contienen secretos reales)
.env.bak.*
.env.*.bak
# Override local para dev (p. ej. publicar el puerto 5432 hacia DBeaver).
# Nunca debe llegar al compose que va a producción.
docker-compose.override.yml

# --- Datos de Usuarios y Temporales ---
data/
output_plot.png

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
venv/
.venv
env/
.env/

# --- Otros ---
.DS_Store
# Datos de salud / archivos estadísticos grandes (Stata, SPSS, CSV masivos)
Test/DBF-ECV-Salud-2025/
*.DTA
*.dta
*.sav
*.SAV
*.csv

# --- Documentacion operativa sensible (IPs, hostnames, topologia) ---
# Se mantiene solo en local; el repo es publico.
docs/VPS-DEPLOY.md
